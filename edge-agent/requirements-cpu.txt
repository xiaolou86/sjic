# ============================================================
# 通用 CPU 或 临时调试 环境 (如个人 PC, 无显卡服务器)
# ============================================================
-r requirements.txt

# 推理引擎 (CPU 模式的 ultralytics)
ultralytics>=8.2.0
torch>=2.6.0
torchvision>=0.15.0

# 常用 CPU 推理加速（可选）
onnxruntime>=1.15.0
